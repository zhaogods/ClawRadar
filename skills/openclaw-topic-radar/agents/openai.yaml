interface:
  display_name: "ClawRadar Topic Radar"
  short_description: "Run the repository's ClawRadar workflow from user topics, real sources, or existing artifacts."
  default_prompt: "Use $openclaw-topic-radar to run the current repository's topic radar workflow with a minimal payload."

policy:
  allow_implicit_invocation: true
